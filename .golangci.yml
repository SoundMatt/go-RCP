version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - copyloopvar
    - misspell
    - bodyclose
    - noctx
  settings:
    errcheck:
      check-type-assertions: true
      exclude-functions:
        - (io.Closer).Close
        - (*os.File).Close
        - os.Remove
        - (*github.com/SoundMatt/go-RCP/mock.Controller).Close
        - (*github.com/SoundMatt/go-RCP/mock.Registry).Close
    govet:
      enable-all: true
      disable:
        - fieldalignment
