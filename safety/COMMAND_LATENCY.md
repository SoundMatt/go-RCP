# Command Latency Safety Evidence

Generated by `TestCommandLatencyProfile` — go-RCP v0.3.0

**Workload duration:** 5s  
**GC pressure:** ~64 MiB/s (64 KiB allocations at ~1 ms interval)  
**Watchdog rate:** 100 Hz (one `CmdWatchdog` Send per zone per tick)  

---

## GSN Safety Argument

**Claim (C-1):** go-RCP command delivery on the mock transport meets the 5000 µs watchdog half-period latency budget under sustained GC pressure.

**Goal (G-1):** Demonstrate that Max(Send latency) < 5000 µs over a 5s workload with ~64 MiB/s GC allocation pressure, with ≥ 10 000 samples.

**Strategy (S-1):** Empirical measurement using the in-process mock controller under realistic GC load. Latency sampled for every `Send` call. Percentiles computed over the full sample set.

**Evidence (E-1):**

### Send latency (CmdWatchdog, 2495 samples)

| Metric | Measured | Threshold | Result |
|--------|----------|-----------|--------|
| P50    | 0 µs   | —         | —      |
| P99    | 12 µs   | —         | —      |
| P99.9  | 39 µs   | —         | —      |
| Max    | 87 µs   | < 5000 µs  | **PASS** |

### Publish→Subscribe delivery latency (2438 samples)

| Metric | Measured |
|--------|----------|
| P50    | 4 µs   |
| P99    | 24 µs   |
| P99.9  | 68 µs   |
| Max    | 79 µs   |

### GC stop-the-world pauses

| Metric            | Value      |
|-------------------|------------|
| GC cycles         | 0         |
| Max STW pause     | 0 µs      |
| Total STW time    | 0 µs      |

**Assumptions (A-1):**

- Transport latency (UDP/TLS) is not characterised here; only the library layer overhead is measured.
- GOMAXPROCS is at the OS default; CPU isolation (cgroups + IRQ affinity) can further reduce jitter.
- The mock controller is single-process in-memory; network latency is absent by design.

**Residual risk (R-1):**

- Real zone controllers operate over a physical Ethernet network, adding non-deterministic latency not measured here.
- GC STW pauses of > 1 ms are possible under extreme allocation rates. The TSN transport (v0.10.0) will use hardware timestamping to compensate.
- This evidence covers the mock transport only. Each transport implementation requires its own latency characterisation before integration.
